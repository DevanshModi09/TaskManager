# Task Manager App 📝

A simple **Task Manager web application** built using **Node.js, Express, and MongoDB**.  
This project allows users to create, view, update, and delete tasks through a RESTful API.

This is my **first backend-focused project**, created to learn Express, MongoDB, and proper backend project structure.

---

## 🚀 Features

- Create a new task
- View all tasks
- View a single task by ID
- Update an existing task
- Delete a task
- RESTful API
- Organized MVC-style structure

---

## 🛠️ Tech Stack

- Node.js
- Express.js
- MongoDB
- Mongoose
- JavaScript
- HTML/CSS (static files)

---

## ⚙️ Installation & Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/DevanshModi09/TaskManager.git
Navigate into the project folder:

bash
Copy code
cd TaskManager
Install dependencies:

bash
Copy code
npm install
Create a .env file in the root directory and add:

env
Copy code
MONGO_URI=your_mongodb_connection_string
PORT=3000
⚠️ The .env file is intentionally not included in this repository for security reasons.

Start the server:

bash
Copy code
npm start
